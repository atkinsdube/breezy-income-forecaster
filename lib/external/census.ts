export async function fetchCompetitionIndex(
  serviceType: string,
  city: string,
  state: string,
): Promise<number> {
  void serviceType;
  void city;
  void state;
  return 0.45;
}

export async function fetchLocalMarketStrength(city: string, state: string): Promise<number> {
  void city;
  void state;
  return 0.58;
}
